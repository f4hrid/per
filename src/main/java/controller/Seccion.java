/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package controller;

import static java.awt.Color.BLACK;
import model.Roles;
import static model.config.REDUV;
import view.Home;
import static view.Home.getButton;

/**
 *
 * @author Fahrid
 */
public class Seccion {
    
    public void setShow(Roles rol, Home h){

        /*
        switch (rol.getRole()){
            case "administrador" -> {
            }
            case "docente" -> {
                h.buttonbox.add(new PanelRound("Boton 1d", REDUV, BLACK));
                h.buttonbox.add(new PanelRound("Boton 2d", REDUV, BLACK));
                h.buttonbox.add(new PanelRound("Boton 3d", REDUV, BLACK));
                h.accesscontrol.add(new PanelRound("Cerrar sesión", REDUV, BLACK));
            }
            case "estudiante" -> {
                h.buttonbox.add(new PanelRound("Boton 1e", REDUV, BLACK));
                h.buttonbox.add(new PanelRound("Boton 2e", REDUV, BLACK));
                h.buttonbox.add(new PanelRound("Boton 3e", REDUV, BLACK));
                h.accesscontrol.add(new PanelRound("Cerrar sesión", REDUV, BLACK));
            }
            default -> {
                h.buttonbox.add(new PanelRound("Boton 1i", REDUV, BLACK));
                h.buttonbox.add(new PanelRound("Boton 2i", REDUV, BLACK));
                h.buttonbox.add(new PanelRound("Boton 3i", REDUV, BLACK));
                h.buttonbox.add(new PanelRound("Boton 4i", REDUV, BLACK));
                h.buttonbox.add(new PanelRound("Boton 5i", REDUV, BLACK));
                h.buttonbox.add(new PanelRound("Boton 6i", REDUV, BLACK));
                h.accesscontrol.add(new PanelRound("Cerrar sesión", REDUV, BLACK));
            }

        }
        */
    }
}
